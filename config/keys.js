// config/keys.js
// Add, edit, or remove allowed API keys here — one string per key.
// No restart/build step needed beyond a redeploy (Vercel redeploys on push).

const VALID_KEYS = [
  "bunny",
  "key123",
  "admin_access",
  "test_key",
];

module.exports = { VALID_KEYS };
